/* 每次读取一行字符串输入 */
//利用getline()来读取一整行，getline(name, nameSize)
//面向行的输入：get()是iostream类中的成员函数，cin.get(name,ArSize)--read first line
//cin.get()--read newline  此时并没有消去换行符
//cin.get(dessert,dessert, ArSize);
//cin.get(name, ArSize).get();--concatenate member functions
//cin.get()返回一个cin对象，该对象随后将被用来调用get()函数
   